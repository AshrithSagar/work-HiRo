%
\documentclass[a4paper, 10pt, conference]{ieeeconf}

\IEEEoverridecommandlockouts{}
\overrideIEEEmargins{}

\usepackage{subfig, booktabs, url}
\usepackage{amsfonts, amsmath, amssymb, bm, cancel, interval, latexsym, mathptmx, mathtools, mathptmx}
\usepackage[colorlinks=true]{hyperref}
\usepackage[ruled,linesnumbered]{algorithm2e}

\title{
    \LARGE \bf
    Motion Generalisation for Dynamical Systems
    \\ [0.5em]
    \large MTech Project Mid-Term Report
}
\author{
    Ashrith Sagar Yedlapalli$^{1}$
    \thanks{$^{1}$ M.Tech in Robotics and Autonomous Systems, IISc Bangalore
    {\tt\small Email: ashrithy@iisc.ac.in}}
}

\begin{document}

\maketitle
\thispagestyle{empty}
\pagestyle{empty}

\begin{abstract}
    Robot learning from demonstrations (LfD) is a promising approach for teaching complex skills to robots without explicit programming.
    However, generalizing these skills to novel situations remains a challenge.
    This report outlines progress in developing motion generalization techniques for dynamical systems in robotics, with a primary focus on the GPT-O (Gaussian Process Transportation with Obstacles) framework.
    GPT-O extends policy transportation methods to incorporate obstacle avoidance through space warping, enabling robust adaptation of learned motions to new environments.
    By leveraging keypoints from demonstrations and obstacles, combined with affine and Gaussian process transforms, GPT-O ensures stable, collision-free trajectory generalization.
    Preliminary demonstrations in 2D and 3D validate its efficacy for tasks like reaching and navigation in cluttered spaces.
\end{abstract}

\begin{keywords}
    Motion generalization,
    Gaussian processes,
    Policy transportation,
    Obstacle avoidance.
\end{keywords}

\section{INTRODUCTION}

Robot learning from demonstrations (LfD) has emerged as a powerful paradigm for teaching robots complex tasks without explicit programming.
In interactive LfD, non-expert users can kinesthetically guide the robot to perform tasks like pick-and-place, dressing, or cleaning.
However, a major challenge is generalizing these learned policies to novel situations, such as varying object positions, arm postures, or surface geometries.

To address generalization, task-parameterized models have been developed to adapt skills based on external parameters, such as reference frames representing objects or goals.
For instance, Task-Parameterized Gaussian Mixture Models (TP-GMM) encode demonstrations in multiple frames and combine them probabilistically for new configurations.
Building on this, Task-Parameterized Gaussian Processes (TPGP)~\cite{ramirez2024learning} leverage Gaussian Processes (GPs) for modeling local policies relative to each frame, with self-supervised learning of frame relevance using uncertainty quantification to improve stability and performance in multi-frame tasks, as demonstrated in simulation and robotic re-shelving experiments.

Recent advancements further enhance adaptability; for example, generalizable motion policies through keypoint parameterization~\cite{lee2024generalizable} enable robust skill transfer across varying environments by parameterizing motions with keypoints and transportation maps. Similarly, Elastic Motion Policies (EMP)~\cite{li2025elastic} provide a dynamical system for one-shot imitation learning, allowing real-time adaptation via Laplacian editing and Lyapunov functions, ensuring convergence in dynamic settings.

Interactive imitation learning algorithms, such as DAgger (Dataset Aggregation), iteratively collect expert corrections to mitigate distribution shift in behavioral cloning.
Variants like HG-DAgger (Human-Gated DAgger) incorporate human oversight to decide when interventions are needed, reducing expert burden in real-world settings.
Compliant Residual DAgger (CR-DAgger) extends this by using residual policies for compliant control, enabling safer and more adaptive learning from converging supervisors.
These methods, surveyed in~\cite{celemin2022interactive}, enhance LfD by combining demonstration with online feedback.

Dynamical systems (DS) model robot motions as stable attractors, facilitating goal convergence and adaptability to perturbations.
Motion generalization in DS involves extrapolating learned policies (e.g., trajectories from demonstrations) to novel scenarios, including varying endpoints and environmental constraints such as obstacles.
This work emphasizes obstacle-aware generalization, addressing limitations in traditional DS where static policies fail in dynamic settings.

The core contribution is GPT-O, an implementation that builds on policy transportation via Gaussian processes (GPs) and affine transforms, integrated with obstacle modulation.
Inspired by keypoint parameterization, GPT-O treats obstacle boundaries as additional keypoints to warp the motion space, preserving DS stability while avoiding collisions.

\section{RELATED WORK}

Movement primitives (MPs) provide compact, reusable representations of robotic motions, enabling efficient imitation learning, generalization, and adaptation.
Dynamic Movement Primitives (DMPs), introduced by Ijspeert et al.~\cite{Ijspeert2013}, model both discrete and rhythmic behaviors as autonomous dynamical systems.
A DMP consists of a canonical linear system generating a phase variable and a transformation system (second-order spring-damper) driven by a learned nonlinear forcing term approximated via locally weighted regression on basis functions.
Demonstrations are encoded into weights, allowing straightforward modulation of amplitude, goal, duration, and coupling with external forces or other primitives—making DMPs a cornerstone for trajectory generation in imitation learning.

Probabilistic Movement Primitives (ProMPs) by Paraschos et al.~\cite{10.5555/2999792.2999904} extend DMPs into a fully probabilistic framework.
Trajectories are represented as a linear combination of radial basis functions whose weights follow a multivariate Gaussian distribution learned from multiple demonstrations.
This captures movement variability, supports stochastic execution, and enables conditioning on via-points or final states through closed-form Bayesian updates.
Kernelized Movement Primitives (KMPs), proposed by Huang et al.~\cite{Huang2019}, replace fixed basis functions with kernel ridge regression, yielding a non-parametric representation.
KMPs naturally handle multiple via-point constraints, obstacle avoidance, and fusion of primitives without explicit phase alignment, offering superior flexibility for complex task adaptation in interactive imitation scenarios.

\subsection{Task-Parameterized Models}

Task-parameterized models address generalization by encoding skills relative to task parameters, such as coordinate frames.
Task-Parameterized Gaussian Mixture Models (TP-GMM), introduced by Calinon~\cite{calinon2016tutorial}, transform demonstrations into multiple reference frames and fit GMMs to each.
At execution, the models are transformed to new frame configurations and combined via Gaussian product to generate adapted trajectories.
This approach enables heuristic-free and segmentation-free learning of multi-frame skills, as shown in applications like manipulation and dressing assistance~\cite{pignat2017learning}.
However, TP-GMM relies on discrete mixtures, which may limit handling of uncertainties.

Task-Parameterized Gaussian Processes (TPGP), proposed by Ramírez Montero et al.~\cite{ramirez2024learning}, extend this framework using GPs.
Local GP policies model dynamics relative to each frame, incorporating progress variables and variance minimization for stability.
Frame relevance is learned self-supervised via another GP, optimizing likelihood on held-out demonstrations.
Evaluated on multi-frame tasks, TPGP outperforms TP-GMM in goal proximity and Fréchet distance metrics, demonstrating robust generalization in simulation and real-world robotic re-shelving.

Recent works like Lee et al.~\cite{lee2024generalizable} propose keypoint-parameterized motion policies with transportation maps for generalizable skill learning.
In this paper, the authors introduce a flexible methodology to generalize robot motion policies using keypoint-based representations and transportation maps.
Tasks are parameterized by manually defined keypoints, and transportation maps are estimated to align source and target keypoint configurations.
The transportation map is composed of an affine transformation followed by a nonlinear Gaussian Process residual, preserving geometric relationships.
This allows generating new motions for novel task configurations while maintaining policy structure.
The approach is validated on tasks like peg insertion and bag opening, showing generalization across geometric variations in robot and task setups.
It enables data-efficient learning and can be integrated with downstream policy methods.
Elastic Motion Policies (EMP)~\cite{li2025elastic} introduce a dynamical system for one-shot imitation learning, adapting via Laplacian editing and online Lyapunov learning for robust performance in dynamic environments.
EMP represents demonstrations as a mesh graph, where nodes are sampled points along the trajectory, and edges connect nearby points to encode local geometry.
Deformations are achieved through Laplacian editing, which minimizes changes to relative positions while satisfying new constraints like altered start/goal points or via-points.
Stability is ensured by learning a Lyapunov function online, guaranteeing convergence to the target attractor even under perturbations.
This approach excels in tasks requiring elasticity, such as cloth manipulation or obstacle avoidance, and outperforms baselines in metrics like success rate and trajectory smoothness in simulated and real-robot experiments.
Compared to keypoint-based methods like GPT-O, EMP emphasizes graph-based flexibility over explicit transportation maps, potentially offering complementary strengths in handling complex deformations.

\subsection{Interactive Imitation Learning}

Interactive imitation learning enhances standard LfD by allowing online expert feedback to correct learner errors.
DAgger (Dataset Aggregation), by Ross et al.~\cite{ross2011reduction}, iteratively trains a policy on an aggregated dataset of states visited by the learner and actions provided by the expert, addressing covariate shift in behavioral cloning.
This no-regret online learning reduction provides strong performance guarantees.

HG-DAgger (Human-Gated DAgger), introduced by Kelly et al.~\cite{kelly2019hg}, modifies DAgger for human experts by gating interventions based on estimated risk or uncertainty, reducing supervision time while maintaining safety in real-world systems like robotics.

Compliant Residual DAgger (CR-DAgger) builds on DAgger with residual policies for compliant control, enabling learning from converging supervisors~\cite{balakrishna2019imitation}.
It focuses on practical implementation in contact-rich tasks, providing guidance for effective data collection and human corrections in real-world robot learning.

These techniques are comprehensively surveyed in~\cite{celemin2022interactive}, highlighting their role in bridging demonstration and reinforcement learning for adaptive robotics.

\section{BACKGROUND}

\subsection{Kabsch Algorithm}

The Kabsch algorithm is a method for finding the optimal rotation matrix that aligns two sets of points in Euclidean space, minimizing the root mean square deviation (RMSD).
It is widely used in structural biology for superimposing molecular structures and in robotics for aligning point clouds or keypoints.

Given two sets of points \(\mathbf{P}\) and \(\mathbf{Q}\) (each with \(n\) points in \(d\)-dimensions), the algorithm proceeds as follows:
\begin{itemize}
    \item Compute the centroids \(\mathbf{c}_P = \frac{1}{n} \sum \mathbf{p}_i\) and \(\mathbf{c}_Q = \frac{1}{n} \sum \mathbf{q}_i\).
    \item Center the points: \(\mathbf{P}' = \mathbf{P} - \mathbf{c}_P\), \(\mathbf{Q}' = \mathbf{Q} - \mathbf{c}_Q\).
    \item Compute the covariance matrix \(\mathbf{H} = \mathbf{P}'^T \mathbf{Q}'\).
    \item Perform singular value decomposition: \(\mathbf{H} = \mathbf{U} \boldsymbol{\Sigma} \mathbf{V}^T\).
    \item The optimal rotation \(\mathbf{R} = \mathbf{V} \mathbf{D} \mathbf{U}^T\), where \(\mathbf{D}\) is a diagonal matrix with 1's and possibly a (-1) to ensure positive determinant (for proper rotation).
    \item The translation is \(\mathbf{t} = \mathbf{c}_Q - \mathbf{R} \mathbf{c}_P\).
\end{itemize}

In GPT-O, the Kabsch algorithm is used in the affine transform to align source and target keypoints, providing the linear component of the transportation map.

\subsection{Gaussian Processes}

Gaussian Processes (GPs) are a non-parametric probabilistic model for regression and function approximation.
A GP defines a distribution over functions \(f(\mathbf{x}) \sim \mathcal{GP}(m(\mathbf{x}), k(\mathbf{x}, \mathbf{x}'))\), where \(m\) is the mean function (often zero) and \(k\) is the kernel function specifying covariance.
GPs can model complex nonlinear functions without fixed basis.
The posterior provides not just predictions but also variance, quantifying confidence.
For a test point \(\mathbf{x}_*\), the predictive distribution is Gaussian with mean \(\bar{f}_* = \mathbf{k}_*^T {(\mathbf{K} + \sigma^2 \mathbf{I})}^{-1} \mathbf{y}\) and variance \(\text{var}(f_*) = k(\mathbf{x}_*, \mathbf{x}_*) - \mathbf{k}_*^T {(\mathbf{K} + \sigma^2 \mathbf{I})}^{-1} \mathbf{k}_*\), where \(\mathbf{K}\) is the kernel matrix on training points.

Common kernels like the radial basis function (RBF) \(k(\mathbf{x}, \mathbf{x}') = \exp\left(-\frac{\|\mathbf{x} - \mathbf{x}'\|^2}{2l^2}\right)\) control smoothness via the lengthscale \(l\).
This uncertainty estimation is crucial for handling noisy data or sparse observations, as higher variance indicates regions where the model is less confident.
In policy transportation, GPs can interpolate smoothly between keypoints while providing derivative information for Jacobians.

In GPT-O, GPs model the nonlinear residual in the transportation map, fitting residuals after affine alignment. The uncertainty helps in handling noisy keypoints, and the Jacobian of the GP (via derivatives) enables velocity transport while preserving stability.
For example, in residual fitting, the GP posterior mean provides the correction term, and its covariance can inform robustness to keypoint perturbations.

\section{METHODOLOGY}

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figs/1.jpeg}
    \caption{
        Single obstacle 2D demo
    }\label{fig:2d-demo-single}
\end{figure}

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figs/2.jpeg}
    \caption{
        Multiple obstacles 2D demo
    }\label{fig:2d-demo-multiple}
\end{figure}

The GPT-O framework builds upon the policy transportation approach described in~\cite{lee2024generalizable}, extending it to incorporate obstacle avoidance through space warping.
In the original paper, tasks are parameterized using ordered sets of keypoints in source and target configurations.
The transportation map \(\phi: \mathbb{R}^d \to \mathbb{R}^d\) is a diffeomorphism that maps source keypoints \(\mathcal{S}\) to target keypoints \(\mathcal{T}\).
It is composed as \(\phi(x) = \gamma(x) + \psi(\gamma(x))\), where \(\gamma\) is an affine transform fitted via Kabsch algorithm, and \(\psi\) is a Gaussian Process for nonlinear residuals.
The Jacobian \(J_\phi\) is used to transport velocities: \(\dot{\hat{x}} = J_\phi(x) \dot{x}\).
This preserves the structure of the dynamical system policy, ensuring stability if \(\det(J_\phi) > 0\).

To handle obstacles, GPT-O treats them as circular (2D) or spherical (3D) entities. For each obstacle, its center is replicated in the source keypoints (tiled to match the number of boundary points), while the boundary points (sampled uniformly on the circumference/surface) serve as corresponding target keypoints.
This setup effectively ``expands'' the obstacle in the warped space, creating a deformation that pushes trajectories away from the obstacle while preserving convergence to the target.

Keypoints are constructed as:
- Source: [start point, tiled obstacle centers, source end point]
- Target: [start point, obstacle boundary points, target end point]

The transportation map is fitted on these keypoints, and the source curve (demonstration trajectory) is warped by applying \(\phi\) to positions and \(J_\phi\) to velocities.

\subsection{Dynamical Systems Integration}

The warped trajectories serve as generalized motions in dynamical systems \(\dot{x} = f(x)\), fitted using methods like DMPs or GPs. Stability is preserved if \(\det(J_\phi) > 0\), ensuring the attractor dynamics remain intact in the target space.
Compared to the original paper, GPT-O adds obstacle keypoints to the parameterization, enabling collision-free generalization without modifying the base policy learning.

\begin{figure}[tbp]
    \centering
    \includegraphics[width=\linewidth]{figs/3.jpeg}
    \caption{
        Single obstacle 3D demo
    }\label{fig:3d-demo-matplotlib}
\end{figure}

\begin{figure}[tbp]
    \centering
    \includegraphics[width=\linewidth]{figs/5.jpeg}
    \caption{
        Single obstacle in 3D space with corresponding warping shown
    }\label{fig:3d-demo-viser}
\end{figure}

\subsection{Jacobian Computation}

The Jacobian \(J_\phi\) of the transportation map \(\phi\) is essential for transporting velocities and ensuring the stability of the dynamical system.
It is computed as the derivative \(J_\phi(\mathbf{x}) = \frac{\partial \phi}{\partial \mathbf{x}}\).
Since \(\phi(\mathbf{x}) = \gamma(\mathbf{x}) + \psi(\gamma(\mathbf{x}))\), the chain rule yields \(J_\phi = J_\gamma + J_\psi \cdot J_\gamma\), where \(J_\gamma\) is the constant affine matrix (including rotation and optional scale), and \(J_\psi\) is the Jacobian of the GP at the transformed points \(\gamma(\mathbf{x})\).

For the GP \(\psi\), assuming a differentiable kernel like RBF \(k(\mathbf{x}, \mathbf{x}') = \sigma^2 \exp\left(-\frac{\|\mathbf{x} - \mathbf{x}'\|^2}{2l^2}\right)\), the predictive mean for a test point \(\mathbf{x}_*\) is \(\bar{\psi}(\mathbf{x}_*) = \mathbf{k}_*^T {(\mathbf{K} + \alpha \mathbf{I})}^{-1} \mathbf{R}\), where \(\mathbf{k}_*\) is the kernel vector between \(\mathbf{x}_*\) and training points, \(\mathbf{K}\) is the training kernel matrix, \(\alpha\) is noise, and \(\mathbf{R}\) are residuals.

The Jacobian \(J_\psi(\mathbf{x}_*)\) (a \(d \times d\) matrix for \(d\)-dimensional space) has entries derived from kernel gradients: \(\frac{\partial \bar{\psi}_i}{\partial x_{*j}} = \sum_m {\left( \frac{\partial k_*^{(m)}}{\partial x_{*j}} \right)}^T {(\mathbf{K} + \alpha \mathbf{I})}^{-1} \mathbf{R}_i\), where \(i\) indexes output dimensions, \(j\) input dimensions, and \(m\) training points.
For RBF, \(\frac{\partial k(\mathbf{x}_*, \mathbf{x}^{(m)})}{\partial x_{*j}} = -\frac{(x_{*j} - x^{(m)}_j)}{l^2} k(\mathbf{x}_*, \mathbf{x}^{(m)})\).

This analytical computation is efficient for moderate keypoint sets (e.g., tens to hundreds) and ensures smooth velocity transport.
In GPT-O, Jacobians are evaluated point-wise for curve points, preserving local differential structure and enabling stable DS generalization even with nonlinear warps.

\begin{algorithm}
    \caption{GPT-O warping}
    \SetAlgoLined{}

    \KwIn{Source curve points $\mathbf{P}$, velocities $\mathbf{V}$, source keypoints $\mathcal{S}$, target keypoints $\mathcal{T}$ (including obstacle boundary points), affine options (scale, rotate)}

    \KwOut{Warped curve $\hat{\mathbf{P}}$, warped velocities $\hat{\mathbf{V}}$}

    Fit affine transform $\gamma$ on $\mathcal{S}$ and $\mathcal{T}$ using Kabsch algorithm\;

    Compute residuals: $\mathbf{R} \gets \mathcal{T} - \gamma(\mathcal{S})$\;

    Fit Gaussian Process $\psi$ on transformed points $\gamma(\mathcal{S})$ with targets $\mathbf{R}$\;

    Define transportation map: $\phi(\mathbf{x}) \gets \gamma(\mathbf{x}) + \psi(\gamma(\mathbf{x}))$\;

    $\hat{\mathbf{P}} \gets \phi(\mathbf{P})$\tcp*{Warp positions}

    Compute Jacobian matrix $J_\phi$ of $\phi$ at points $\mathbf{P}$\;

    $\hat{\mathbf{V}} \gets J_\phi \mathbf{V}$\tcp*{Transport velocities}

    \Return{$\hat{\mathbf{P}}, \hat{\mathbf{V}}$}
\end{algorithm}

\section{PRELIMINARY RESULTS \& DISCUSSIONS}

Simulations demonstrate GPT-O's effectiveness in generalizing motions while avoiding obstacles:

\begin{itemize}
    \item \textbf{2D Single Obstacle Demo}: A cubic source curve (200 points) is transported to 100 varying endpoints around a circular obstacle (center at (2.0, 0.6), radius 0.15). Warped trajectories, visualized with plasma colormap, avoid collisions and converge smoothly. Interactive mode (Matplotlib) enables real-time obstacle dragging, updating warps instantly for dynamic testing.

    \item \textbf{2D Multiple Obstacles Demo}: Extended to three obstacles (centers at (1.5, 0.4), (2.0, 0.6), (2.8, 0.3); radius 0.15 each). Warped curves adapt to cluttered spaces, maintaining stability.

    \item \textbf{3D Single Obstacle Demo}: A helical source curve (200 points) is warped around a spherical obstacle (center at (2.0, 0.6, 0.0), radius 0.1) to a new endpoint. Visualizations (Matplotlib) show deformed space via grid lines, with color-coded displacements highlighting warp effects.

    \item \textbf{3D Interactive Space Warp Demo}: Using Viser, grid lines are warped in real-time with movable obstacles and endpoints, demonstrating space deformation for intuitive understanding.
\end{itemize}

These results confirm GPT-O's ability to generalize DS motions in obstructed environments, outperforming baseline transportation without obstacles (e.g., direct affine/GP fits, which collide).

\section{CONCLUSIONS}

GPT-O advances motion generalization in DS by integrating obstacle-aware warping with policy transportation, enabling adaptable robot behaviors.
Future work includes real-robot experiments (e.g., UR5 manipulator for simple reaching tasks), multi-modal policies (position + orientation via quaternions), and integration with full DS controllers for closed-loop control.
Additionally, exploring combinations with EMP for elastic deformations and online adaptation could further improve performance in dynamic scenarios, such as by incorporating graph-based editing into the warping process.

\addtolength{\textheight}{-12cm}

\section*{APPENDIX}

\subsection{Notation Summary}

Table~\ref{tab:notation} summarizes key notations used throughout.

\begin{table}[h]
    \centering
    \caption{
        Key Notations
    }\label{tab:notation}
    \begin{tabular}{cl}
        \toprule
        Symbol & Description \\
        \midrule
        \(\mathbf{P}\), \(\mathbf{V}\) & Source curve points and velocities \\
        \(\mathcal{S}\), \(\mathcal{T}\) & Source and target keypoints \\
        \(\gamma\) & Affine transformation (Kabsch-fitted) \\
        \(\psi\) & Gaussian Process residual model \\
        \(\phi\) & Full transportation map \(\phi = \gamma + \psi \circ \gamma\) \\
        \(J_\phi\) & Jacobian of \(\phi\) \\
        \(\hat{\mathbf{P}}\), \(\hat{\mathbf{V}}\) & Warped points and velocities \\
        \bottomrule
    \end{tabular}
\end{table}

\subsection{Example Keypoint Construction}

For a single circular obstacle in 2D with center \(\mathbf{c} = (2.0, 0.6)\) and radius 0.15, boundary points are sampled as \(\mathbf{b}_i = \mathbf{c} + r (\cos \theta_i, \sin \theta_i)\) for \(\theta_i = 2\pi i / n\), \(i=0\dots n-1\) (e.g., \(n=20\)).
Source keypoints tile \(\mathbf{c}\) \(n\) times; targets use \(\mathbf{b}_i\).

\appendix

\section*{ACKNOWLEDGMENT}

I would like to thank my guide, Dr.~Ravi Prakash, for his guidance, insights and support, and for providing me with the opportunity to work on this project. I also thank my colleage, Mr.~Shreyas Kumar from HiRo Lab for his support in helping me get started with the project.

\newpage
\begin{thebibliography}{1}

    \bibitem{calinon2016tutorial}
    S. Calinon, ``A tutorial on task-parameterized movement learning and retrieval'' \emph{Intelligent Service Robotics}, vol. 9, no. 1, pp. 1--29, 2016.

    \bibitem{pignat2017learning}
    E. Pignat and S. Calinon, ``Learning adaptive dressing assistance from human demonstration'' \emph{Robotics and Autonomous Systems}, vol. 93, pp. 61--75, 2017.

    \bibitem{ramirez2024learning}
    M. Ramírez Montero, G. Franzese, J. Kober, and C. Della Santina, ``Learning Multi-Reference Frame Skills from Demonstration with Task-Parameterized Gaussian Processes'' in \emph{2024 IEEE/RSJ International Conference on Intelligent Robots and Systems (IROS)}, 2024.

    \bibitem{ross2011reduction}
    S. Ross, G. J. Gordon, and D. Bagnell, ``A reduction of imitation learning and structured prediction to no-regret online learning'' in \emph{Proceedings of the Fourteenth International Conference on Artificial Intelligence and Statistics}, 2011, pp. 627--635.

    \bibitem{kelly2019hg}
    M. Kelly, C. Sidrane, C. Finn, M. Balakrishna, and Z. Erickson, ``HG-DAgger: Interactive Imitation Learning with Human Experts'' in \emph{2019 International Conference on Robotics and Automation (ICRA)}, 2019.

    \bibitem{balakrishna2019imitation}
    A. Balakrishna, et al., ``On-Policy Robot Imitation Learning from a Converging Supervisor'' in \emph{Conference on Robot Learning (CoRL)}, 2019.

    \bibitem{celemin2022interactive}
    C. Celemin, et al., ``Interactive Imitation Learning in Robotics: A Survey'' \emph{Foundations and Trends in Robotics}, vol. 10, no. 1--2, pp. 1--197, 2022.

    \bibitem{lee2024generalizable}
    S. Lee, et al., ``Generalizable Motion Policies through Keypoint Parameterization and Transportation Maps'' arXiv preprint arXiv:2404.13458, 2024.

    \bibitem{li2025elastic}
    T. Li, S. Sun, S. S. Aditya, and N. Figueroa, ``Elastic Motion Policy: An Adaptive Dynamical System for Robust and Efficient One-Shot Imitation Learning'' arXiv preprint arXiv:2503.08029, 2025.

    \bibitem{Ijspeert2013}
    A. J. Ijspeert, J. Nakanishi, H. Hoffmann, P. Pastor, and S. Schaal, ``Dynamical Movement Primitives: Learning Attractor Models for Motor Behaviors'' \emph{Neural Computation}, vol. 25, no. 2, pp. 328--373, Feb. 2013.

    \bibitem{10.5555/2999792.2999904}
    A. Paraschos, C. Daniel, J. Peters, and G. Neumann, ``Probabilistic Movement Primitives'' in \emph{Proceedings of the 27th International Conference on Neural Information Processing Systems --- Volume 2}, Curran Associates Inc., 2013, pp. 2616--2624.

    \bibitem{Huang2019}
    Y. Huang, L. Rozo, J. Silvério, and D. G. Caldwell, ``Kernelized movement primitives'' \emph{The International Journal of Robotics Research}, vol. 38, no. 7, pp. 833--852, May 2019.

\end{thebibliography}

\end{document}
